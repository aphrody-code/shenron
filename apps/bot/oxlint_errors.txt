
  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
    ,-[src/dashboard/components/RoleSelect.tsx:77:22]
 76 |     if (!includeEveryone) list = list.filter((r) => r.name !== "@everyone");
 77 |     return [...list].sort((a, b) => b.position - a.position);
    :                      ^^^^
 78 |   }, [data, excludeManaged, includeEveryone]);
    `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
    ,-[src/dashboard/components/ChannelSelect.tsx:64:34]
 63 |     }
 64 |     const sortedCats = [...cats].sort((a, b) => (a.position ?? 0) - (b.position ?? 0));
    :                                  ^^^^
 65 |     return { byCat, sortedCats };
    `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  x eslint(no-useless-escape): Unnecessary escape character '-'
    ,-[scripts/ingest/import-github-games.ts:19:17]
 18 |         .replace(/\s+/g, "-")
 19 |         .replace(/[^\w\-]+/g, "")
    :                       ^^
 20 |         .replace(/\-\-+/g, "-");
    `----
  help: Replace `\-` with `-`.

  x eslint(no-useless-escape): Unnecessary escape character '-'
    ,-[scripts/ingest/import-github-games.ts:20:13]
 19 |         .replace(/[^\w\-]+/g, "")
 20 |         .replace(/\-\-+/g, "-");
    :                   ^^
 21 | }
    `----
  help: Replace `\-` with `-`.

  x eslint(no-useless-escape): Unnecessary escape character '-'
    ,-[scripts/ingest/import-github-games.ts:20:15]
 19 |         .replace(/[^\w\-]+/g, "")
 20 |         .replace(/\-\-+/g, "-");
    :                     ^^
 21 | }
    `----
  help: Replace `\-` with `-`.

  ! promise(always-return): Each then() should return a value or throw
    ,-[scripts/ingest/ingest-fandom-transformations.ts:77:15]
 76 |         runTransformationIngest()
 77 | ,->         .then(() => {
 78 | |               const dbs = container.resolve(DatabaseService);
 79 | |               dbs.close();
 80 | `->         })
 81 |             .catch((err) => {
    `----

  x eslint(no-useless-escape): Unnecessary escape character '-'
    ,-[scripts/ingest/import-github-tools.ts:19:17]
 18 |         .replace(/\s+/g, "-")
 19 |         .replace(/[^\w\-]+/g, "")
    :                       ^^
 20 |         .replace(/\-\-+/g, "-");
    `----
  help: Replace `\-` with `-`.

  x eslint(no-useless-escape): Unnecessary escape character '-'
    ,-[scripts/ingest/import-github-tools.ts:20:13]
 19 |         .replace(/[^\w\-]+/g, "")
 20 |         .replace(/\-\-+/g, "-");
    :                   ^^
 21 | }
    `----
  help: Replace `\-` with `-`.

  x eslint(no-useless-escape): Unnecessary escape character '-'
    ,-[scripts/ingest/import-github-tools.ts:20:15]
 19 |         .replace(/[^\w\-]+/g, "")
 20 |         .replace(/\-\-+/g, "-");
    :                     ^^
 21 | }
    `----
  help: Replace `\-` with `-`.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
     ,-[scripts/reconstruct-from-discord.ts:130:52]
 129 | console.log("Distribution par niveau récupéré :");
 130 | for (const [lvl, count] of Object.entries(byLevel).sort(
     :                                                    ^^^^
 131 |     (a, b) => Number(a[0]) - Number(b[0]),
     `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! oxc(no-map-spread): Spreading to modify object properties in `map` calls is inefficient
     ,-[src/services/MessageTemplateService.ts:213:17]
 212 |         await this.ensureFresh();
 213 |         return EVENTS.map((def) => {
     :                       ^|^
     :                        `-- This map call spreads an object
 214 |             const tmpl = this.templateCache.get(def.event);
 215 |             return {
 216 |                 ...def,
     :                 ^^^|^^
     :                    `-- This spread allocates a new value on each iteration
 217 |                 template: tmpl?.template ?? def.defaultTemplate,
     `----
  help: If in-place mutation is acceptable, use `Object.assign` or direct property assignment instead of spreading
  note: `Object.assign` mutates the first argument. Disable this rule if copy-on-write behavior is required.

  ! unicorn(prefer-add-event-listener): Prefer `addEventListener()` over their `on`-function counterparts.
    ,-[src/dashboard/lib/event-stream.ts:50:8]
 49 | 
 50 |     es.onmessage = (msg) => {
    :        ^^^^^^^^^
 51 |       try {
    `----
  help: `addEventListener()` can register multiple handlers and accepts options such as `{ once: true }`; assigning to `on<event>` replaces any previously registered handler.

  ! unicorn(prefer-add-event-listener): Prefer `addEventListener()` over their `on`-function counterparts.
    ,-[src/dashboard/lib/event-stream.ts:65:8]
 64 | 
 65 |     es.onerror = () => {
    :        ^^^^^^^
 66 |       // EventSource gère automatiquement la reconnexion (3s par défaut)
    `----
  help: `addEventListener()` can register multiple handlers and accepts options such as `{ once: true }`; assigning to `on<event>` replaces any previously registered handler.

  ! promise(always-return): Each then() should return a value or throw
    ,-[scripts/ingest/ingest-characters.ts:95:15]
 94 |         runCharacterIngest()
 95 | ,->         .then(() => {
 96 | |               const dbs = container.resolve(DatabaseService);
 97 | |               dbs.close();
 98 | `->         })
 99 |             .catch((err) => {
    `----

  ! promise(always-return): Each then() should return a value or throw
    ,-[scripts/ingest/ingest-fandom-planets.ts:71:15]
 70 |         runPlanetIngest()
 71 | ,->         .then(() => {
 72 | |               const dbs = container.resolve(DatabaseService);
 73 | |               dbs.close();
 74 | `->         })
 75 |             .catch((err) => {
    `----

  ! promise(always-return): Each then() should return a value or throw
    ,-[scripts/ingest/ingest-fandom-characters.ts:83:15]
 82 |         runFandomIngest()
 83 | ,->         .then(() => {
 84 | |               const dbs = container.resolve(DatabaseService);
 85 | |               dbs.close();
 86 | `->         })
 87 |             .catch((err) => {
    `----

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
     ,-[scripts/recover-from-discord-history.ts:403:4]
 402 |     [...userAgg.entries()]
 403 |         .sort((a, b) => b[1].messageCount - a[1].messageCount)
     :          ^^^^
 404 |         .slice(0, 10)
     `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
     ,-[src/commands/moderation/Moderation.ts:936:8]
 935 |     const top3 = Object.entries(stats.byAction)
 936 |       .sort((a, b) => b[1] - a[1])
     :        ^^^^
 937 |       .slice(0, 5)
     `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! promise(always-return): Each then() should return a value or throw
    ,-[scripts/ingest/seed-arcs.ts:76:15]
 75 |         runArcSeed()
 76 | ,->         .then(() => {
 77 | |               const dbs = container.resolve(DatabaseService);
 78 | |               dbs.close();
 79 | `->         })
 80 |             .catch((err) => {
    `----

  ! eslint(no-underscore-dangle): Unexpected dangling '_' in '`__done`'.
    ,-[scripts/optimize-jpeg-batch.ts:59:10]
 58 |             // @ts-expect-error settled state
 59 |             if (p.__done) {
    :                   ^^^^^^
 60 |                 pending.splice(i, 1);
    `----
  help: Remove the dangling '_' or add `__done` to the 'allow' configuration.

  ! eslint(no-underscore-dangle): Unexpected dangling '_' in '`__done`'.
    ,-[scripts/optimize-jpeg-batch.ts:68:5]
 67 |         // @ts-expect-error mark
 68 |         p.__done = true;
    :           ^^^^^^
 69 |     });
    `----
  help: Remove the dangling '_' or add `__done` to the 'allow' configuration.

  x eslint(no-useless-escape): Unnecessary escape character '-'
    ,-[scripts/ingest/bxc-ingest.ts:43:80]
 42 |                 if (exists.length === 0) {
 43 |                     const slug = title.toLowerCase().trim().replace(/\s+/g, "-").replace(/[^\w\-]+/g, "");
    :                                                                                               ^^
 44 |                     await db.insert(dbNews).values({
    `----
  help: Replace `\-` with `-`.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
     ,-[src/commands/economy/Shop.ts:171:6]
 170 |                 .filter((i) => i.roleId)
 171 |                 .sort((a, b) => b.price - a.price);
     :                  ^^^^
 172 |             for (const it of withRole) {
     `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
    ,-[scripts/scan-ids.ts:56:3]
 55 | const channels = [...guild.channels.cache.values()]
 56 |     .sort(
    :      ^^^^
 57 |         (a, b) =>
    `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
    ,-[scripts/scan-ids.ts:77:3]
 76 |     .filter((r) => r.name !== "@everyone")
 77 |     .sort((a, b) => b.position - a.position)
    :      ^^^^
 78 |     .map((r) => ({
    `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
    ,-[src/commands/admin/Ids.ts:47:6]
 46 |                 .filter((r) => r.name !== "@everyone")
 47 |                 .sort((a, b) => b.position - a.position);
    :                  ^^^^
 48 |             const lines = roles.map((r) => {
    `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
    ,-[src/commands/admin/Ids.ts:58:6]
 57 |                 .filter((c) => "position" in c)
 58 |                 .sort(
    :                  ^^^^
 59 |                     (a, b) =>
    `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! promise(always-return): Each then() should return a value or throw
     ,-[src/db/seed-techniques.ts:131:9]
 130 |         runTechniqueSeed()
 131 | ,->         .then(() => {
 132 | |               const dbs = container.resolve(DatabaseService);
 133 | |               dbs.close();
 134 | `->         })
 135 |             .catch((err) => {
     `----

  x eslint(no-useless-escape): Unnecessary escape character '-'
    ,-[src/db/seed-techniques.ts:72:43]
 71 |             // Pattern 1: <li><b>Name</b>: Desc
 72 |             const liRegex = /<li><b>(.*?)<\/b>\s*[:\-]?\s*(.*?)(?=<\/li>|<li>|$)/gs;
    :                                                    ^^
 73 |             let match;
    `----
  help: Replace `\-` with `-`.

  ! promise(always-return): Each then() should return a value or throw
    ,-[src/db/seed-manga.ts:67:9]
 66 |         runMangaSeed()
 67 | ,->         .then(() => {
 68 | |               const dbs = container.resolve(DatabaseService);
 69 | |               dbs.close();
 70 | `->         })
 71 |             .catch((err) => {
    `----

  ! promise(always-return): Each then() should return a value or throw
     ,-[src/db/seed-wiki.ts:477:9]
 476 |         runWikiSeed({ force, log: console.log })
 477 | ,->         .then(() => {
 478 | |               const dbs = container.resolve(DatabaseService);
 479 | |               dbs.close();
 480 | `->         })
 481 |             .catch((err) => {
     `----

  ! eslint(no-underscore-dangle): Unexpected dangling '_' in '`_auth`'.
    ,-[src/lib/better-auth.ts:30:5]
 29 | // callsite est suffisant pour les tests d'autocomplétion en aval.
 30 | let _auth: any = null;
    :     ^^^^^
 31 | 
    `----
  help: Remove the dangling '_' or add `_auth` to the 'allow' configuration.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
    ,-[scripts/scan-github.ts:70:53]
 69 | 
 70 |         const sortedRepos = Array.from(allRepos.values()).sort((a, b) => b.stargazers_count - a.stargazers_count);
    :                                                           ^^^^
 71 | 
    `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! promise(always-return): Each then() should return a value or throw
    ,-[src/db/seed-games.ts:50:9]
 49 |         runGameSeed()
 50 | ,->         .then(() => {
 51 | |               const dbs = container.resolve(DatabaseService);
 52 | |               dbs.close();
 53 | `->         })
 54 |             .catch((err) => {
    `----

  ! promise(always-return): Each then() should return a value or throw
    ,-[src/db/seed-media.ts:70:9]
 69 |         runMediaSeed()
 70 | ,->         .then(() => {
 71 | |               const dbs = container.resolve(DatabaseService);
 72 | |               dbs.close();
 73 | `->         })
 74 |             .catch((err) => {
    `----

  ! promise(always-return): Each then() should return a value or throw
    ,-[scripts/sync-news.ts:78:8]
 77 |     main()
 78 | ,->     .then(() => {
 79 | |           const dbs = container.resolve(DatabaseService);
 80 | |           dbs.close();
 81 | `->     })
 82 |         .catch((err) => {
    `----

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
     ,-[scripts/discover-ids.ts:199:28]
 198 | console.log(`${bold}── Rôles (${roles.length}) ──${reset}`);
 199 | for (const r of [...roles].sort((a, b) => b.position - a.position)) {
     :                            ^^^^
 200 |     if (r.name === "@everyone") continue;
     `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
     ,-[scripts/discover-ids.ts:211:50]
 210 | console.log(`\n${bold}── Salons (${channels.length}) ──${reset}`);
 211 | for (const [type, list] of [...byType.entries()].sort(([a], [b]) => a - b)) {
     :                                                  ^^^^
 212 |     const label = TYPE_LABEL[type] ?? `type-${type}`;
     `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
     ,-[scripts/discover-ids.ts:214:23]
 213 |     console.log(`  ${dim}[${label}]${reset}`);
 214 |     for (const c of list.sort((a, b) => a.position - b.position)) {
     :                          ^^^^
 215 |         console.log(`    ${c.id}  ${c.name}`);
     `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
     ,-[src/api/server.ts:184:4]
 183 |         .filter(([k]) => !k.startsWith("_")) // ignore cache busters
 184 |         .sort(([a], [b]) => a.localeCompare(b));
     :          ^^^^
 185 |     return url.pathname + (params.length ? "?" + new URLSearchParams(params).toString() : "");
     `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
      ,-[src/api/server.ts:1023:8]
 1022 |                         .filter((r) => r.id !== guild.id && !r.managed)
 1023 |                         .sort((a, b) => b.position - a.position)
      :                          ^^^^
 1024 |                         .map((r) => ({
      `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

  ! unicorn(no-array-sort): Use `Array#toSorted()` instead of `Array#sort()`.
      ,-[src/api/server.ts:3915:49]
 3914 |     const url = new URL(req.url);
 3915 |     const params = [...url.searchParams.entries()].sort(([a], [b]) => a.localeCompare(b));
      :                                                    ^^^^
 3916 |     return url.pathname + (params.length ? "?" + new URLSearchParams(params).toString() : "");
      `----
  help: `Array#sort()` mutates the original array. Use `Array#toSorted()` to return a new sorted array without modifying the original.

Found 34 warnings and 8 errors.
Finished in 97ms on 190 files with 138 rules using 12 threads.
