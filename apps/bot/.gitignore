
# Artefacts générés (mirror assets, export JSON, corpus RAG) — VPS only
data/export/
data/rag/
# Cache des flux HLS résolus (tokens éphémères ~12 h, regénéré par le timer) — VPS only
data/streams.json
assets/ext/
assets/wiki/
# Scans manga téléchargés (couleur Full Color + N&B self-hostés) — VPS only,
# régénérés par scripts/ingest-fullcolor-manga.ts & scripts/selfhost-manga-pages.ts
assets/manga/
