[install]
# Force exact versions in lockfile
exact = false

[run]
# Preload reflect-metadata before every bun run for tsyringe decorators
preload = ["./src/lib/preload.ts"]

[test]
preload = ["./src/lib/preload.ts"]
