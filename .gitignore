node_modules/
.env
*.log

# Build artifacts (dist exclu sauf packages vendorés ci-dessous)
apps/*/dist/
!packages/*/dist/

# Bun
.bun/

# SQLite data (runtime) — JAMAIS committer (contient la data utilisateurs / PII)
*.db
*.sqlite
*.sqlite3
*.db-journal
*.db-wal
*.db-shm
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Exports runtime sensibles (PII membres Discord) — régénérables, jamais committer
apps/bot/data/guild-scan.json
apps/bot/data/.recovery-checkpoint.json
apps/bot/data/backups/
**/guild-scan*.json
**/.recovery-checkpoint.json

# Drizzle generated migrations are committed, but keep cache out
.drizzle/

# n2b audit reports
bun/reports/

# AI assistants — configs, instructions, caches
.claude/
.cursor/
.cursorrules
.aider*
.continue/
.windsurf/
.codeium/
.sourcegraph/
.anthropic/
.openai/
.github/copilot-instructions.md
CLAUDE.md
AGENTS.md
GEMINI.md
COPILOT.md
.ai/
*.ai.md

# Backgrounds générés — bun scripts/fetch-backgrounds.ts --download && bun scripts/optimize-backgrounds.ts
assets/backgrounds/
.npmrc
.bun-cache/
styles.compiled.css

# Turborepo
.turbo/
**/.turbo/

# Workspace artifacts
apps/*/node_modules/
apps/*/dist/
apps/*/.next/
apps/*/data/*.db*
packages/*/node_modules/
# packages/*/dist/  --  committed for vendored @rpbey/* until build pipeline est setup

# Python __pycache__ (gemini tooling)
__pycache__/

# Coord sprint artifacts (local only, ne pas committer le state)
.coord/messages.jsonl
.coord/memory/
.gemini/tmp/
.vercel

# AI agents skills cache (better-auth, etc.)
.agents/
skills-lock.json

# Recon / extraction artefacts (lourd, hors repo)
reference/
apps/bot/public/db/
scratch/


# transformers.js model cache (RAG embeddings)
apps/bot/.models/
.env*.local

# LLM maison — artefacts générés sur la machine (entraînés en local, comme l'index RAG ; non commités)
apps/bot/data/llm/.venv/
apps/bot/data/llm/*.pt
apps/bot/data/llm/*.gguf
apps/bot/data/llm/corpus.txt
apps/bot/data/llm/sft.jsonl
apps/bot/data/llm/dbz-sft.jsonl
apps/bot/data/llm/dbz_tokenizer.json
apps/bot/data/llm/dbz_model_config.json
apps/bot/data/llm/_tok_train.txt
